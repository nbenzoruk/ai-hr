# 🚂 Railway Setup - Пошаговая инструкция (Вариант B)

**Время:** ~15 минут
**Результат:** 3 работающих сервиса + PostgreSQL

---

## 📋 Что будем делать

1. ✅ Удалить старый сервис
2. ✅ Добавить PostgreSQL базу
3. ✅ Создать Backend сервис
4. ✅ Создать Candidate Portal
5. ✅ Создать HR Panel
6. ✅ Протестировать

---

## 🗑️ ШАГ 1: Удалить старый сервис

1. Открой Railway Dashboard: https://railway.app/dashboard
2. Кликни на проект **"natural-energy"** (или как он называется)
3. Найди сервис **"ai-hr"** (который упал)
4. Кликни на него
5. Справа вверху → **Settings** (⚙️)
6. Прокрути вниз → **Danger Zone**
7. **Delete Service** → Подтверди

**Результат:** Проект пустой, только база данных (если была)

---

## 🗄️ ШАГ 2: Добавить PostgreSQL

1. В проекте нажми **"+ New"** (большая кнопка)
2. Выбери **"Database"**
3. Выбери **"Add PostgreSQL"**
4. Railway автоматически создаст базу

**Результат:** Видишь сервис "Postgres" с зелёной галочкой ✅

**Важно:** Запомни имя сервиса (обычно `Postgres` или `postgres`)

---

## ⚙️ ШАГ 3: Создать Backend сервис

### 3.1 Создание сервиса

1. Нажми **"+ New"**
2. Выбери **"GitHub Repo"**
3. Найди и выбери **"ai-hr"** репозиторий
4. Railway начнёт деплой (и упадёт - это нормально)

### 3.2 Настройка Builder

1. Кликни на новый сервис (назовётся `ai-hr`)
2. Переименуй его в **"backend"** (Settings → Service Name)
3. Перейди в **Settings → Build**
4. Измени настройки:
   ```
   Builder: Dockerfile
   Dockerfile Path: docker/backend.Dockerfile
   Root Directory: /
   ```
5. **Save Changes**

### 3.3 Настройка переменных окружения

1. В этом же сервисе перейди во вкладку **"Variables"**
2. Добавь 3 переменные (кнопка **"+ New Variable"**):

   **Переменная 1:**
   ```
   Variable Name: OPENAI_API_KEY
   Value: sk-proj-ВАШ_КЛЮЧ_ОТ_OPENAI
   ```

   **Переменная 2:**
   ```
   Variable Name: DATABASE_URL
   Value: ${{Postgres.DATABASE_URL}}
   ```
   ⚠️ **Важно:** Если база называется по-другому, замени `Postgres` на её имя

   **Переменная 3:**
   ```
   Variable Name: ENVIRONMENT
   Value: production
   ```

3. **Save** (сохраняется автоматически)

### 3.4 Настройка Networking

1. Перейди в **Settings → Networking**
2. Кликни **"Generate Domain"**
3. Railway создаст публичный URL типа:
   ```
   https://backend-production-XXXX.up.railway.app
   ```
4. **СКОПИРУЙ ЭТОТ URL** - он понадобится для frontend!

### 3.5 Редеплой

1. Перейди во вкладку **"Deployments"**
2. Нажми на последний деплой
3. Справа вверху → **⋮** (три точки) → **"Redeploy"**
4. Дождись завершения (смотри логи)

**Результат:** Backend должен запуститься с зелёной галочкой ✅

**Проверка:** Открой `https://твой-backend-url.up.railway.app/docs` - должна открыться Swagger UI

---

## 🎯 ШАГ 4: Создать Candidate Portal

### 4.1 Создание сервиса

1. Нажми **"+ New"** (в проекте)
2. **"GitHub Repo"** → **"ai-hr"** (да, снова тот же репозиторий)
3. Переименуй сервис в **"frontend-candidate"**

### 4.2 Настройка Builder

1. **Settings → Build**
2. Настройки:
   ```
   Builder: Dockerfile
   Dockerfile Path: docker/frontend.Dockerfile
   Root Directory: /
   ```
3. **Build Arguments:** (внизу страницы)
   - Нажми **"+ Add Build Argument"**
   - Добавь:
     ```
     Key: APP_FILE
     Value: app_candidate.py
     ```
4. **Save**

### 4.3 Настройка переменных

1. Перейди в **"Variables"**
2. Добавь 2 переменные:

   **Переменная 1:**
   ```
   Variable Name: BACKEND_URL
   Value: https://твой-backend-url.up.railway.app
   ```
   ⚠️ **Замени на реальный URL** из Шага 3.4!

   **Переменная 2:**
   ```
   Variable Name: DEMO_MODE
   Value: false
   ```

### 4.4 Настройка Networking

1. **Settings → Networking**
2. **"Generate Domain"**
3. Скопируй URL (понадобится для отправки HR-у)

### 4.5 Редеплой

1. **Deployments** → последний деплой → **⋮** → **"Redeploy"**
2. Дождись завершения

**Результат:** Candidate Portal работает ✅

**Проверка:** Открой URL - должна загрузиться страница с воронкой

---

## 👔 ШАГ 5: Создать HR Panel

### 5.1 Создание сервиса

1. **"+ New"** → **"GitHub Repo"** → **"ai-hr"** (третий раз)
2. Переименуй в **"frontend-hr"**

### 5.2 Настройка Builder

1. **Settings → Build**
   ```
   Builder: Dockerfile
   Dockerfile Path: docker/frontend.Dockerfile
   Root Directory: /
   ```
2. **Build Arguments:**
   ```
   Key: APP_FILE
   Value: app_hr.py
   ```

### 5.3 Настройка переменных

1. **Variables**
2. Добавь:
   ```
   Variable Name: BACKEND_URL
   Value: https://твой-backend-url.up.railway.app
   ```

### 5.4 Настройка Networking

1. **Settings → Networking** → **"Generate Domain"**
2. Скопируй URL

### 5.5 Редеплой

1. **Deployments** → **⋮** → **"Redeploy"**

**Результат:** HR Panel работает ✅

**Проверка:** Открой URL - должна загрузиться админ панель

---

## ✅ ШАГ 6: Финальная проверка

### Чеклист

- [ ] **Backend** - статус ✅, `/docs` открывается
- [ ] **Candidate Portal** - статус ✅, страница загружается
- [ ] **HR Panel** - статус ✅, страница загружается
- [ ] **Postgres** - статус ✅

### Тест воронки

1. Открой **HR Panel**
2. Создай тестовую вакансию
3. Скопируй ссылку
4. Открой **Candidate Portal**
5. Пройди первые 2-3 этапа
6. Вернись в **HR Panel** → проверь что кандидат появился

---

## 📝 Твои ссылки (запиши)

```
Backend API:
https://_______________________________.up.railway.app

Candidate Portal (для кандидатов):
https://_______________________________.up.railway.app

HR Panel (для HR):
https://_______________________________.up.railway.app
```

---

## 🐛 Troubleshooting

### Backend не стартует

**Логи показывают ошибку:**
```
sqlalchemy.exc.OperationalError: could not connect to database
```

**Решение:**
1. Проверь переменную `DATABASE_URL`
2. Должна быть: `${{Postgres.DATABASE_URL}}`
3. Если база называется по-другому - замени `Postgres`

---

### Frontend показывает ошибку "Connection refused"

**Причина:** Неправильный `BACKEND_URL`

**Решение:**
1. Проверь переменную `BACKEND_URL` в frontend сервисах
2. Должна быть: `https://твой-backend-url.up.railway.app`
3. БЕЗ слэша в конце!
4. Протокол `https://`, не `http://`

---

### Build Arguments не работают

**Симптом:** Frontend сервисы используют один и тот же app файл

**Решение:**
1. Settings → Build
2. Убедись что **Build Arguments** добавлены:
   - Candidate: `APP_FILE=app_candidate.py`
   - HR: `APP_FILE=app_hr.py`
3. Redeploy

---

## 🎉 Готово!

Теперь можешь отправить ссылки своему HR-у для тестирования:

```
Привет! Вот тестовая версия AI-HR системы:

🎯 Портал для кандидатов:
https://твой-candidate-url.up.railway.app

👔 Панель для HR (здесь создавай вакансии):
https://твой-hr-url.up.railway.app

Попробуй создать вакансию и пройти воронку!
```

---

**Вопросы?** Пиши в чат, помогу разобраться!
