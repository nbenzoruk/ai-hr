# Railway Environment Variables Template
# Copy these to Railway Dashboard → Variables

# ============================================
# BACKEND SERVICE
# ============================================

# OpenAI API Key (REQUIRED)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Database URL (Railway автоматически создаст)
# Формат: postgresql+asyncpg://user:password@host:port/database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Environment
ENVIRONMENT=production

# ============================================
# FRONTEND CANDIDATE SERVICE
# ============================================

# Backend URL (ссылка на backend сервис)
BACKEND_URL=${{backend.RAILWAY_PUBLIC_DOMAIN}}

# Demo mode (можно отключить в production)
DEMO_MODE=false

# ============================================
# FRONTEND HR SERVICE
# ============================================

# Backend URL (ссылка на backend сервис)
BACKEND_URL=${{backend.RAILWAY_PUBLIC_DOMAIN}}

# ============================================
# NOTES
# ============================================
# 1. Railway автоматически заменит ${{...}} переменные
# 2. PostgreSQL будет создана автоматически при добавлении сервиса
# 3. Каждый сервис нужно настроить отдельно в Dashboard
# 4. OPENAI_API_KEY нужно добавить вручную для backend сервиса
